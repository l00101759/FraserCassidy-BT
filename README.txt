Run from the command line 
using: "node program.js packages.txt foo bar"

Any number of extra packages can be added onto this command

Program requires more than "node program.js '__.txt' " to run
It must include a txt file as the third parameter





